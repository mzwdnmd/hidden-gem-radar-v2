# 小馆雷达 V4 地图拖影与性能优化任务书（Luna Max 执行版）

## 1. 任务目标

修复 V4 地图在拖动、缩放时出现的强烈拖影、瓦片跳变和加载缓慢问题，并降低底图道路细节。保留现有真实高德 POI、城市切换、地图选点、餐馆标记、均匀分布和“移动地图后手动搜索”机制。

本任务只优化地图渲染链路，不改推荐算法，不改餐馆筛选规则，不重做页面布局。

## 2. 已确认的症状与根因

重点文件：

- `components/real-map.tsx`
- `app/globals.css`

当前实现同时存在两套底图：

1. `AMap.TileLayer` 被传入 `AMap.Map({ layers: [...] })`。
2. React 组件 `RasterBaseLayer` 另外生成大量 `<img>` 瓦片，放在高德画布下面作为兜底。

已确认的具体问题：

### 2.1 拖影

`RasterBaseLayer` 仅在 `moveend`、`zoomend`、`resize` 后同步中心点和缩放级别。拖动或缩放进行中，兜底瓦片保持在旧位置，高德标记和交互画布已经移动。两者短时间错位，形成明显拖影；事件结束后整层突然跳到新位置。

### 2.2 加载慢和闪烁

瓦片节点的 key 包含 `zoom-x-y`。中心或缩放改变后，React 会批量卸载旧瓦片并创建新瓦片。新图片尚未下载完成时会出现空块、旧图残留或闪烁。

页面还保留了高德 `TileLayer`，可能造成重复渲染与重复网络开销。即使该图层在当前环境没有成功绘出，也不应继续和兜底层并存。

### 2.3 底图过细

地图默认缩放为 15，兜底层按相同级别请求瓦片。当前产品用于观察候选店的空间分布，不需要展示大量支路、小路和建筑级路网。高细节瓦片增加请求数量、视觉噪声和重绘成本。

### 2.4 React 状态参与高频地图渲染

`rasterView` 是 React state。地图事件触发后会带动组件重新渲染，并重新计算所有瓦片。高频视觉同步不应依赖整棵 React 子树反复更新。

## 3. 目标架构

最终只能保留一套可见底图渲染链路。

优先方案：使用高德地图自身管理的单一瓦片图层，让底图和地图手势共用同一个变换矩阵。修复并验证自定义 `AMap.TileLayer` 的配置后，删除 React `RasterBaseLayer`、`RasterView` 以及对应 `<img>` 图层。

如果当前运行环境仍无法让高德图层可靠绘制，才启用备用方案：保留一个独立的轻量瓦片层，但必须改成命令式、双缓冲、随手势连续变换的实现。不要在 `mousemove`、`mapmove` 或缩放动画期间用 React state 重建瓦片列表。

## 4. 实施顺序

Luna Max 必须按以下顺序执行。每一步完成后先在浏览器验证，再进入下一步。

### 阶段 A：建立可复现基线

1. 打开当前成都地图。
2. 记录一次水平拖动、一次连续缩放的表现。
3. 在开发者工具中确认：
   - 当前可见底图来自哪一层；
   - 单次拖动或缩放新建了多少瓦片节点；
   - 是否同时存在高德 canvas 图层和 `.real-map-raster`；
   - 是否出现重复瓦片请求、失败请求或长时间 pending。
4. 不要凭视觉猜测修复完成，必须以 DOM、网络请求和最终截图共同判断。

### 阶段 B：先尝试单一高德图层

1. 修正 `AMap.TileLayer` 初始化方式，使其能够独立显示道路底图。
2. 地图对象只配置这一套底图，不再渲染 `<RasterBaseLayer>`。
3. 高德图层确认能稳定显示后，删除：
   - `RasterView` 类型；
   - `rasterView` state；
   - `syncRasterLayer`；
   - `RasterBaseLayer` 函数；
   - `.real-map-raster` 相关 CSS。
4. 不得以同时保留两套底图作为容错方案。
5. 若官方图层在目标浏览器仍为空，记录实际错误或缺失能力，再执行阶段 C。

### 阶段 C：备用轻量瓦片层

仅当阶段 B 被实际验证为不可用时执行。

1. 用一个专门的命令式图层控制器管理瓦片 DOM，可放在独立模块中，例如 `lib/amap-raster-layer.ts`。
2. 图层控制器通过 ref 直接更新 DOM transform，不用 React state 逐帧驱动。
3. 拖动期间：
   - 旧瓦片容器必须随地图实时平移；
   - 使用 `requestAnimationFrame` 合并连续事件；
   - 每帧最多执行一次样式更新；
   - 不在拖动期间重新生成整批 `<img>`。
4. 缩放期间：
   - 旧瓦片先用 CSS transform 连续缩放；
   - 新层在后台加载；
   - 新层达到可展示条件后一次性交换；
   - 交换完成后再释放旧层，避免白屏和跳闪。
5. 设置两层缓存上限，禁止历史瓦片容器无限累积。
6. 对相同 `z/x/y` 复用节点或缓存 URL，避免重复请求。
7. 图片使用 `decoding="async"`、`draggable="false"`，图层保持 `pointer-events: none`。
8. 组件卸载时移除事件监听、取消动画帧、清理计时器和瓦片节点。

## 5. 简化底图要求

产品需要的是区域和主干路参照，不追求建筑级路网。

请采用以下策略：

1. 将地图初始缩放从 15 调整为 13 或 14，以能看清城区结构且不过度展示支路为准。
2. 城市切换后的默认缩放统一使用同一规则。
3. 将允许缩放范围控制在适合餐馆发现的区间，建议 `zooms: [11, 16]`。
4. 使用高德标准简洁底图参数；不要启用卫星、实时交通、3D 建筑、室内图或额外道路覆盖层。
5. 若使用备用瓦片层，可让视觉瓦片级别比交互缩放低一级，再按比例放大显示，以降低道路细节和请求量；必须保证道路位置与标记位置对齐。
6. 不要用 CSS 模糊隐藏细节，模糊会降低文字清晰度并增加合成开销。

## 6. 性能约束

1. 同一时刻只能有一套正式底图和最多一套正在预载的交换层。
2. 拖动期间不得触发餐馆 API 请求，继续保留“搜索此区域”按钮。
3. 拖动期间不得批量创建或销毁瓦片节点。
4. 地图平移只做 transform；瓦片补充安排在手势结束后或浏览器空闲阶段。
5. 缩放与移动监听器必须节流到 `requestAnimationFrame`。
6. 避免因底图事件反复执行餐馆标记密度计算；密度计算应在稳定帧或交互结束后执行。
7. 保留已有标记选中态、点击事件和无障碍名称。
8. 不新增大型地图框架依赖；优先使用现有高德 JS API 和少量本地代码。

## 7. 代码边界

允许修改：

- `components/real-map.tsx`
- `app/globals.css`
- 必要时新增一个轻量地图图层工具模块及其测试

不得修改：

- 推荐评分公式
- 黑名单及连锁店规则
- 高德 POI 查询接口及真实数据字段
- 用户标注数据结构
- “地图移动不自动刷新”的产品规则
- 城市输入功能

## 8. 验收场景

必须在实际浏览器中完成以下验收：

### 8.1 首屏

- 输入“成都”并查看。
- 底图能在可接受时间内出现，无整片空白。
- 可看到主要道路和区域关系，不出现过量小路细节。
- 餐馆标记与道路位置匹配。

### 8.2 拖动

- 连续向左、向右各拖动一次。
- 手指或鼠标移动期间，底图与标记保持同步。
- 不出现旧路网停在原位、新路网叠上来的双影。
- 松手后不发生整张底图明显跳变。
- 不自动调用餐馆搜索接口。

### 8.3 缩放

- 连续放大两级，再缩小两级。
- 过渡过程中允许短暂降低清晰度，但不能出现白屏、双层道路或大面积瓦片闪烁。
- 缩放完成后，标记位置与底图重新精确对齐。

### 8.4 网络与 DOM

- 可见底图只有一套。
- 不存在长期累积的历史瓦片节点。
- 来回拖回已看过区域时，应复用浏览器缓存，不能重复爆发相同请求。
- 控制台无新增错误和 React key 警告。

### 8.5 回归

- 城市切换正常。
- “搜索此区域”正常。
- 定位按钮正常。
- 点击餐馆标记可切换详情。
- 均匀显示开关正常。
- 地图移动后只更新候选区域提示，不自动消耗 POI 调用额度。

## 9. 自动检查

修改完成后必须运行并通过：

1. ESLint。
2. 正式构建。
3. 已有测试；若项目没有图层测试，为瓦片坐标换算、缓存淘汰或图层交换逻辑补充最小单元测试。

## 10. Luna Max 输出要求

完成任务后输出以下内容：

1. 最终根因确认，说明实际生效的是阶段 B 还是阶段 C。
2. 修改文件列表。
3. 删除了哪些旧的双层渲染代码。
4. 新的底图渲染和缓存机制说明。
5. 拖动、缩放、城市切换三个场景的浏览器验证结果。
6. ESLint、构建和测试结果。
7. 仍存在的限制，禁止用“已经优化”代替可核验的数据和现象。

## 11. 完成定义

同时满足以下条件才算完成：

- 底图不再空白；
- 拖动和缩放无明显拖影；
- 不再同时运行两套可见底图；
- 路网细节明显降低；
- 加载速度和瓦片稳定性改善；
- 地图移动不触发餐馆数据自动刷新；
- 所有自动检查通过；
- 实际浏览器验收通过。
