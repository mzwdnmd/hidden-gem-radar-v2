# 小馆雷达 V4 版本修改说明书（Luna 执行版）

## 0. 执行说明

本文档用于指导 Luna 模型直接修改现有 V3 项目。执行时以当前工作区代码为准，不重建项目，不替换技术栈，不删除 V3 已有能力。

项目目录：`D:\chy;\hidden-gem-radar-v2`

主要文件：

- `app/page.tsx`：页面状态、筛选、标注和详情面板。
- `components/real-map.tsx`：高德地图、点位和地图交互。
- `lib/restaurant-types.ts`：餐馆及接口类型。
- `app/api/restaurants/route.ts`：高德餐馆查询与原始数据转换。
- `app/api/features/route.ts`：环境特征查询。
- `app/globals.css`：页面样式。

Luna 执行要求：

1. 先阅读上述文件，再开始修改。
2. 每个阶段完成后运行检查，确认通过后再进入下一阶段。
3. 优先使用小型纯函数，不在 `page.tsx` 中继续堆积复杂算法。
4. 保留真实数据原则。不得添加模拟餐馆、模拟评论、模拟经营年限或虚构评分。
5. 高德评分仍允许 `3.0～4.7`，不得恢复 4.2 分门槛。
6. 距地图中心不得参与候选准入和推荐得分。
7. 地图移动不得自动请求高德 POI；只有用户明确搜索时请求。
8. 不开发大众点评爬虫，不绕过验证码、登录、反爬或动态字体。
9. 当前版本先实现透明规则评分和训练数据积累，不在浏览器内训练 LightGBM。
10. 不进行无关的视觉重构，不批量改写现有 UI 组件。

---

## 1. V4 目标

V4 完成以下四项能力：

1. 自动屏蔽茶府、麻将、棋牌等非目标娱乐场所。
2. 用户标记一家连锁门店后，屏蔽同一品牌的全部门店。
3. 使用独立、透明、可解释的“反平台排序”公式，不按高德评分直接排序。
4. 地图点位按屏幕空间抽稀，优先展示高分候选，避免局部大量覆盖。

V4 完成后的用户可见结果：

- 详情页显示“候选分”，并显示构成候选分的有效证据和缺失项。
- 高德评分仍原样显示，并明确标注来源。
- 被娱乐规则、品牌黑名单、单店黑名单过滤的商户可通过“显示已屏蔽”查看。
- 地图显示的点位数量少于或等于右侧候选列表；右侧列表不得因地图抽稀而丢失候选。
- 地图移动只重新计算显示点位，不产生新的餐馆查询请求。

---

## 2. 建议新增文件

为降低 `page.tsx` 的复杂度，新增以下文件：

```text
lib/brand-normalizer.ts
lib/restaurant-filter.ts
lib/recommendation-score.ts
lib/map-density.ts
lib/v4-storage.ts
```

职责必须保持单一：

- `brand-normalizer.ts`：名称清洗、品牌主体提取、品牌匹配。
- `restaurant-filter.ts`：娱乐类型、黑名单和连锁品牌过滤。
- `recommendation-score.ts`：特征归一化、候选分计算和解释。
- `map-density.ts`：根据地图缩放和屏幕坐标选出需要显示的点位。
- `v4-storage.ts`：V3 数据迁移、V4 本地存储读写、异常数据保护。

不要在本阶段引入数据库服务。继续使用 `localStorage`，但数据结构必须带版本号并支持导出。

---

## 3. 类型与数据结构

### 3.1 扩展餐馆类型

在 `lib/restaurant-types.ts` 中增加：

```ts
export type RecommendationFeature = {
  key: string;
  label: string;
  value: number | null;
  weight: number;
  contribution: number;
  source: "amap" | "user" | "derived";
  note: string;
};

export type RecommendationScore = {
  score: number;
  confidence: number;
  risk: number;
  features: RecommendationFeature[];
  missingFeatures: string[];
  formulaVersion: "v4-rule-1";
};

export type FilterReason =
  | "entertainment_type"
  | "entertainment_name"
  | "restaurant_blacklist"
  | "chain_brand_blacklist";
```

不要把派生候选分写入高德原始 `rating` 字段。

### 3.2 V4 标注记录

```ts
export type V4RestaurantLabel = {
  restaurantId: string;
  provider: "amap";
  originalName: string;
  normalizedName: string;
  canonicalBrand: string | null;
  labels: Array<"want" | "blacklist" | "chain" | "liked" | "average" | "not_interested">;
  reasons: string[];
  note: string;
  restaurantSnapshot: Restaurant;
  createdAt: string;
  updatedAt: string;
};
```

### 3.3 品牌黑名单记录

```ts
export type BrandBlacklistEntry = {
  id: string;
  canonicalName: string;
  aliases: string[];
  sourceRestaurantIds: string[];
  sourceNames: string[];
  createdBy: "user";
  createdAt: string;
  updatedAt: string;
};
```

### 3.4 本地存储键

统一使用：

```text
hidden-gem-v4-state
```

顶层结构：

```ts
type V4State = {
  version: 1;
  restaurantLabels: Record<string, V4RestaurantLabel>;
  brandBlacklist: BrandBlacklistEntry[];
  entertainmentWhitelist: string[];
  settings: {
    showFiltered: boolean;
    densityEnabled: boolean;
  };
};
```

首次加载 V4 时读取 `hidden-gem-v3-labels`，把已有 `want / blacklist / chain` 转换为 V4 结构。迁移成功后保留 V3 原键，不删除用户旧数据。迁移过程失败时显示提示，不能清空原记录。

---

## 4. 娱乐场所过滤

### 4.1 强过滤关键词

首版关键词使用：

```ts
const ENTERTAINMENT_NAME_PATTERNS = [
  /茶府/u,
  /茶楼/u,
  /棋牌/u,
  /麻将/u,
  /桌游/u,
  /网吧/u,
  /网咖/u,
  /台球/u,
  /KTV/iu,
  /歌舞厅/u,
  /足浴/u,
  /洗浴/u,
  /娱乐会所/u,
];
```

### 4.2 分类过滤

同时检查 `restaurant.type`、`restaurant.category` 和 `restaurant.tags`。分类或标签包含下列词时过滤：

```text
棋牌、麻将、网吧、网咖、KTV、歌舞厅、台球、足浴、洗浴、娱乐场所
```

不要只依靠名称，也不要只依靠高德分类。

### 4.3 白名单

在“显示已屏蔽”状态下，为规则屏蔽的商户提供“这是一家餐馆，恢复显示”操作。点击后把 POI ID 放入 `entertainmentWhitelist`。白名单优先级高于娱乐规则，但低于用户明确设置的单店黑名单。

过滤优先级：

```text
用户单店黑名单
> 用户品牌黑名单
> 用户娱乐场所白名单
> 自动娱乐规则
> 普通候选
```

页面要显示明确原因，例如“名称含‘棋牌’，已由娱乐场所规则屏蔽”。

---

## 5. 连锁品牌整体屏蔽

### 5.1 品牌名称规范化

在 `lib/brand-normalizer.ts` 实现纯函数：

```ts
normalizeRestaurantName(name: string): string
extractCanonicalBrand(name: string): string
isSameBrand(name: string, entry: BrandBlacklistEntry): boolean
```

规范化顺序固定如下：

1. Unicode `NFKC` 标准化。
2. 英文字母转小写。
3. 去除首尾空格和连续空格。
4. 将中文括号、英文括号统一处理。
5. 仅当括号内容明显是分店信息时，删除括号及内容。
6. 删除结尾的分店描述。
7. 删除不影响品牌的分隔符，保留中文、字母和数字。

可删除的分店信息关键词：

```text
店、分店、总店、旗舰店、直营店、加盟店、概念店、万达店、广场店、大学城店、路店、街店、商场店
```

建议规则：

```ts
/[（(][^）)]*(?:店|分店|总店|旗舰店|直营店|加盟店)[）)]\s*$/u
/(?:总店|分店|旗舰店|直营店|加盟店)\s*$/u
```

不要删除定义品牌的品类词，例如“鸡公煲”“火锅”“拉面”“烧烤”。

示例：

```text
XX鸡公煲（万达店） -> xx鸡公煲
XX鸡公煲(大学城分店) -> xx鸡公煲
XX鸡公煲旗舰店 -> xx鸡公煲
老王火锅 -> 老王火锅
```

### 5.2 用户操作

用户点击“连锁店”时：

1. 计算建议品牌主体。
2. 打开确认框，展示原店名和建议品牌名。
3. 允许用户修改品牌名。
4. 确认后将品牌加入 `brandBlacklist`。
5. 当前结果和以后查询到的同品牌门店立即过滤。

用户取消连锁标注时，只移除该 POI 的 `chain` 标签，不能直接删除品牌黑名单。品牌黑名单必须在独立的“已屏蔽品牌”区域删除，防止一次误触恢复全部门店。

### 5.3 匹配规则

V4 只自动屏蔽高置信匹配：

```text
候选规范名 === canonicalName
或
候选规范名 === aliases 中任一规范名
或
候选名称以 canonicalName 开头，剩余部分全部是明确分店描述
```

不要仅凭低阈值编辑距离自动屏蔽。相似名称只能显示为“疑似同品牌”，交给用户确认。这样可以避免把同名小店误判为连锁。

### 5.4 管理界面

在详情侧栏增加可折叠的“已屏蔽品牌”区域，展示：

- 品牌主体。
- 已命中的门店数量。
- 来源门店。
- 添加时间。
- 删除品牌黑名单按钮。

---

## 6. V4 透明候选分

### 6.1 基本原则

- 候选分名称固定为“候选分”，不得称为平台评分或真实评分。
- 高德评分权重固定为 `0.06`。
- 评分较低本身不产生奖励。
- 缺失特征不按 0 分计算，必须重新归一化有效权重。
- 没有合法来源的数据不得推测填写。
- 当前接口没有评论正文、评论数量、菜品分或经营年限时，对应特征保持 `null`。

### 6.2 特征定义

全部特征取值范围为 `0～1`：

```text
F_food       菜品口碑证据
F_repeat     本地客/回头客证据
F_years      经营年限
F_value      性价比
F_exposure   低曝光但有证据
F_gap        菜品分相对平台总分优势
F_amap       高德评分证据
F_profile    用户个人偏好概率
```

风险特征：

```text
R_recent       近期差评上升
R_hygiene      卫生或食品安全风险
R_price        价格异常
R_surrounding  周边环境负面影响
R_uncertainty  数据不确定性
```

### 6.3 基础分公式

理论完整权重：

```text
B =
0.28 × F_food
+ 0.18 × F_repeat
+ 0.14 × F_years
+ 0.12 × F_value
+ 0.12 × F_exposure
+ 0.10 × F_gap
+ 0.06 × F_amap
```

实际计算时只使用非空特征。设有效特征集合为 `A`：

```text
B_available = Σ(w_j × F_j) / Σ(w_j), j ∈ A
```

为保持 V1 的反平台排序特征，实际基础分还要乘以“有效证据覆盖率”：

```text
B = 0.5 + (B_available - 0.5) × Σ(w_j), j ∈ A
```

因此只有高德评分时，评分只会让候选分轻微偏离中性值 50；评论、菜品、经营年限、性价比和环境等真实证据逐步补齐后，排序差异才会扩大。

如果当前只有高德评分和人均消费，页面必须写明“当前证据有限”，不能假装已经使用评论或经营年限。

### 6.4 子特征公式

高德评分证据：

```text
F_amap = clamp((rating - 3.0) / 1.7, 0, 1)
```

经营年限：

```text
F_years = min(ln(1 + years) / ln(13), 1)
```

低曝光但有证据：

```text
F_exposure =
(1 - 同城同品类评论数百分位)
× sqrt(reviewCount / (reviewCount + 20))
```

菜品优势：

```text
F_gap = clamp((foodRating - platformRating) / 1.0, 0, 1)
```

性价比仅在存在真实人均消费且能计算同城同品类中位数时启用：

```text
F_value = clamp(1 - averageCost / (2 × categoryMedianCost), 0, 1)
```

只有一家店或样本少于 5 家时，`F_value = null`。

### 6.5 风险分

```text
R =
0.35 × R_recent
+ 0.25 × R_hygiene
+ 0.15 × R_price
+ 0.15 × R_surrounding
+ 0.10 × R_uncertainty
```

缺失风险特征同样重新归一化。没有任何可靠风险证据时，`R = 0`，同时降低置信度；不得凭空生成风险。

### 6.6 个人偏好和最终分

V4 暂不训练机器学习模型。根据用户标注数量决定 `λ`：

```text
有效偏好标注少于 30 条：λ = 0
30～49 条：λ = 0.05
50～99 条：λ = 0.10
100 条及以上：λ = 0.15
上限：0.25
```

在尚未训练模型时，`F_profile = 0.5`，不改变分数。

```text
T = clamp(B + λ × (F_profile - 0.5) - 0.30 × R, 0, 1)
```

置信度：

```text
C = clamp(
0.30 × dataCompleteness / 100
+ 0.50 × 有效基础特征权重和
+ 0.20 × 风险证据完整度,
0,
1
)
```

最终候选分：

```text
S = round(50 + C × (100 × T - 50))
```

`S` 限制在 `0～100`。证据较少时分数向 50 收缩。

### 6.7 当前真实数据不足时的行为

V4 当前很可能只有高德评分、人均消费、资料完整度和环境特征。因此必须做到：

- 不生成评论数量。
- 不生成菜品分。
- 不生成回头客比例。
- 不生成经营年限。
- 环境特征只有用户点击“分析周边”后才可参与展示。
- 首次候选分可能接近 50，并显示“低置信度”。这是正确结果。

### 6.8 排序和展示

默认候选列表按以下顺序：

```text
候选分降序
→ 置信度降序
→ 高德评分降序
→ 名称稳定排序
```

详情页展示：

- 候选分。
- 置信度百分比。
- 高德真实评分。
- 正向贡献最大的 3 个有效特征。
- 风险贡献最大的 2 个有效特征。
- 缺失特征列表。
- 公式版本 `v4-rule-1`。

移除或改写当前“为什么进入候选”中可能直接复述高德分层的内容。不能把高德评分包装成算法结论。

---

## 7. 地图点位均匀分布

### 7.1 两套集合

必须区分：

```text
visibleRestaurants：通过筛选、用于列表和排序的全部候选
mapRestaurants：经过地图抽稀、实际创建 Marker 的候选
```

地图抽稀不得改变右侧列表，不得删除数据，也不得改变候选分。

### 7.2 地图接口补充

在 `components/real-map.tsx` 的地图类型中补充实际需要的方法：

```ts
getZoom(): number;
lngLatToContainer(position: [number, number]): { getX(): number; getY(): number };
```

监听：

```text
zoomend
moveend
resize
```

这些事件只更新本地布局版本并重新计算 `mapRestaurants`，不能调用 `/api/restaurants`。

### 7.3 最小像素间距

```text
d(z) = clamp(48, 110 - 10 × (z - 12), 110)
```

参考值：

```text
zoom 12 -> 110 px
zoom 15 -> 80 px
zoom 18 -> 50 px
```

### 7.4 抽稀算法

在 `lib/map-density.ts` 实现可测试的贪心算法。输入为已经按候选分排序的屏幕点：

```ts
type DensityPoint<T> = {
  id: string;
  x: number;
  y: number;
  score: number;
  item: T;
};
```

伪代码：

```text
按 score 降序、id 升序稳定排序
accepted = []

for point in sortedPoints:
  如果 point 是当前选中店：接受
  否则如果 point 与 accepted 中每个点的距离都 >= d(z)：接受
  否则：放入 hiddenByDensity

返回 accepted 和 hiddenByDensity
```

距离公式：

```text
distance = sqrt((x1 - x2)^2 + (y1 - y2)^2)
```

选中的餐馆永远显示。若它与其他点重叠，选中点的层级最高，允许临时替换附近低分点。

### 7.5 密度提示

地图底部显示：

```text
当前区域共 N 家候选 · 地图显示 M 家 · 放大地图可查看更多
```

当 `N === M` 时只显示“当前区域共 N 家候选”。

“显示已屏蔽”与“地图抽稀”是两种状态，文案不能混淆。

### 7.6 是否使用 Supercluster

V4 第一阶段优先实现上述本地像素抽稀，不强制安装依赖。原因是当前点位数量有限，目标是均匀显示高价值候选。

若实测单次返回超过 500 个点，再引入：

```text
supercluster
@types/supercluster
```

引入后仍需保留候选分优先和选中点强制显示规则。不得为了使用库而改变产品规则。

---

## 8. 页面修改清单

### 8.1 顶部与标题

- 产品版本改为 `V4`。
- 副标题可改为 `EXPLAINABLE DISCOVERY EDITION`。
- 导出文件名改为 `hidden-gem-radar-v4-labels-YYYY-MM-DD.json`。
- 导出内容包含 V4 状态、公式版本和导出时间。

### 8.2 地图筛选区

保留：

- 城市输入。
- 分类。
- 搜索半径。
- “搜索此区域”。
- “显示已屏蔽”。
- “区域已变化”。

增加：

- “均匀显示点位”开关，默认开启。
- 候选总数与地图显示数量。

### 8.3 店铺选择列表

- 按候选分排序。
- 每行显示候选分和高德评分，两者标签必须分开。
- 被规则过滤的店在“显示已屏蔽”模式下使用弱化样式并显示过滤原因。

### 8.4 店铺详情

增加：

- 候选分卡片。
- 置信度。
- 公式解释。
- 缺失证据。
- 过滤原因。
- 娱乐误判恢复按钮。

保留：

- 高德评分、人均消费、地址、电话、营业时间。
- 环境特征。
- 外部商户链接。
- 数据来源和更新时间。

### 8.5 连锁标注

点击“连锁店”后必须先确认品牌主体，不允许无提示地直接加入品牌黑名单。

确认界面字段：

```text
原门店名：只读
建议品牌主体：可编辑
说明：确认后将屏蔽当前及未来查询到的同品牌门店
取消 / 确认屏蔽品牌
```

---

## 9. 文件级实施顺序

Luna 按以下顺序执行，不要跳步。

### 阶段 A：基础类型与存储

1. 扩展 `lib/restaurant-types.ts`。
2. 新建 `lib/v4-storage.ts`。
3. 完成 V3 到 V4 的只读迁移。
4. 修改导出格式。
5. 运行类型检查或构建。

### 阶段 B：品牌和过滤

1. 新建 `lib/brand-normalizer.ts`。
2. 新建 `lib/restaurant-filter.ts`。
3. 接入娱乐规则。
4. 接入品牌黑名单。
5. 增加连锁品牌确认交互。
6. 增加娱乐场所白名单恢复操作。
7. 运行 lint 和构建。

### 阶段 C：候选分

1. 新建 `lib/recommendation-score.ts`。
2. 逐项实现归一化函数。
3. 实现缺失特征权重重分配。
4. 实现置信度收缩。
5. 在列表和详情中展示候选分。
6. 修改默认排序。
7. 运行 lint 和构建。

### 阶段 D：地图抽稀

1. 新建 `lib/map-density.ts`。
2. 扩展高德地图类型。
3. 监听缩放、移动和尺寸变化。
4. 计算屏幕坐标。
5. 只为 `mapRestaurants` 创建 Marker。
6. 保证选中点始终显示。
7. 增加数量提示和开关。
8. 运行 lint 和构建。

### 阶段 E：回归检查

1. 用真实高德数据打开页面。
2. 检查城市搜索。
3. 检查地图移动不发出 POI 请求。
4. 检查刷新和搜索仍由按钮触发。
5. 检查本地标注刷新后仍在。
6. 检查 V3 标注迁移。
7. 检查导出 JSON。
8. 检查无数据、接口限额和密钥缺失状态。

---

## 10. 必须覆盖的测试用例

如果项目当前没有测试框架，至少把纯函数设计成可测试形式，并进行手动验证。不要为了本次任务引入大型测试框架。

### 10.1 品牌规范化

```text
XX鸡公煲（万达店） == xx鸡公煲
XX鸡公煲(大学城分店) == xx鸡公煲
XX鸡公煲旗舰店 == xx鸡公煲
老王火锅 == 老王火锅
茶府人家餐厅不得被规范化为空字符串
```

### 10.2 娱乐规则

```text
好运棋牌室 -> entertainment_name
友谊麻将馆 -> entertainment_name
某某KTV -> entertainment_name
某某餐厅，分类含棋牌 -> entertainment_type
白名单中的同一 POI -> 不被自动娱乐规则隐藏
用户黑名单中的白名单 POI -> 仍被黑名单隐藏
```

### 10.3 品牌黑名单

```text
将“XX鸡公煲（万达店）”标记为品牌 xx鸡公煲
“XX鸡公煲（大学城店）”必须隐藏
“XX鸡公煲旗舰店”必须隐藏
“XX鸡煲仔饭”不得仅凭相似而自动隐藏
删除品牌黑名单后，同品牌门店恢复普通候选状态
```

### 10.4 候选分

```text
rating = 3.0 时 F_amap = 0
rating = 4.7 时 F_amap = 1
rating 缺失时不得把 F_amap 当成 0
只有一个有效特征时按该特征归一化计算
低置信度结果应向 50 收缩
所有结果必须处于 0～100
相同输入必须得到相同结果
```

### 10.5 地图抽稀

```text
两个相距 20 px 的点只保留候选分较高者
两个相距 120 px 的点都显示
当前选中点即使分数较低也必须显示
放大地图后显示数量不得无故减少
关闭均匀显示后，地图显示全部可见候选
移动和缩放地图不得调用 /api/restaurants
```

---

## 11. 验收标准

全部满足才视为 V4 完成：

- [ ] 茶府、棋牌、麻将等娱乐场所默认被过滤。
- [ ] 自动过滤支持单店白名单恢复。
- [ ] 标记一家连锁门店时会确认品牌主体。
- [ ] 同品牌分店在当前结果和以后查询中均被屏蔽。
- [ ] 单店黑名单与品牌黑名单独立保存。
- [ ] V3 标注成功迁移且原数据未被删除。
- [ ] 高德评分 3.0～4.7 仍可进入候选。
- [ ] 距地图中心不参与准入和候选分。
- [ ] 候选分严格使用 `v4-rule-1` 公式。
- [ ] 缺失特征会重新分配权重，不会被当作零分。
- [ ] 详情页能解释候选分和置信度。
- [ ] 高德评分与候选分在界面上清楚区分。
- [ ] 地图点位分布明显减少遮挡。
- [ ] 右侧列表保留全部未过滤候选。
- [ ] 选中的餐馆始终有地图标记。
- [ ] 拖动或缩放地图不触发新的高德 POI 请求。
- [ ] 用户点击“搜索此区域”后才查询新区域。
- [ ] 无真实评论时不展示虚构评论分析。
- [ ] `pnpm lint` 通过。
- [ ] `pnpm build` 通过。

---

## 12. 禁止事项

Luna 不得进行以下操作：

- 不把所有高德评分较低的店自动视为宝藏店。
- 不用 `distanceMeters` 筛掉候选或参与候选分。
- 不根据名称相似就大范围自动屏蔽品牌。
- 不在地图移动事件中调用餐馆 API。
- 不把地图抽稀后的数量当作候选总数。
- 不把派生分写成“高德评分”“大众点评评分”或“真实评分”。
- 不虚构评论、评论数、菜品分、经营年限和回头客数据。
- 不把人工黑名单样本从存储中物理删除。
- 不删除 V3 本地存储键。
- 不加入未经授权的大众点评自动抓取。
- 不为了接入 GitHub 项目复制整套框架或改换地图服务。

---

## 13. GitHub 借鉴边界

可参考以下项目的思想和公开 API，不整仓复制：

- `mapbox/supercluster`：大量点位的空间聚类；V4 点位超过 500 时再引入。
- `uber/h3-js`：后续城市级六边形密度特征；本轮不强制接入。
- `osmlab/name-suggestion-index`：品牌主体、别名和适用地区的数据结构。
- `rapidfuzz/RapidFuzz`：名称相似匹配思想；V4 自动屏蔽仍只使用高置信规则。
- `lightgbm-org/LightGBM`：未来有足够用户标签后的学习排序。
- `shap/shap`：未来模型特征贡献解释。

V4 当前实现范围：规则过滤、品牌库、透明公式、点位抽稀和训练数据积累。机器学习训练放到数据量达到要求后的版本。

---

## 14. 完成后交付内容

Luna 完成修改后必须提供：

1. 修改过的文件列表。
2. 每个文件的主要变化。
3. 公式实现位置和公式版本。
4. 本地存储迁移说明。
5. 已执行的检查命令及结果。
6. 尚未具备真实数据来源的特征清单。
7. 已知限制和下一步建议。

不要只回复“已完成”。必须说明实际完成范围，并明确任何没有实现或无法验证的部分。
