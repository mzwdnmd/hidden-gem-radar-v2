# 小馆雷达 V3 版本修改方案

## 1. 版本目标

V3 将从“按地图位置展示餐馆”升级为“人工标注驱动的低价值目标过滤与候选发现系统”。

核心目标：

1. 候选准入不受“距地图中心”影响。
2. 建立黑名单、连锁店和用户偏好三类人工标签，为后续机器学习提供训练数据。
3. 地图移动后由用户主动触发搜索，控制高德接口调用额度。
4. 为每家店采集道路、周边设施、人均消费等环境特征。
5. 保留真实评分、原始评论和来源证据，方便人工判断。
6. 为大众点评等第三方数据预留合规的数据连接层。

---

## 2. 候选规则调整

### 2.1 移除地图中心距离的准入作用

- 距地图中心不参与候选过滤、排序、评分和推荐理由。
- 距离仍可作为普通信息展示，便于用户判断出行成本。
- 默认排序优先使用平台真实评分、人工偏好和模型预测结果。
- 用户可以手动选择按距离排序，但该操作不改变候选资格。

### 2.2 评分范围

- 保留高德真实评分 3.0–4.7 的候选。
- 无评分店铺单独进入“资料不足”分组，不直接视为低价值目标。
- 页面不得生成容易被误认为平台数据的综合评分。
- 派生指标必须明确标注为“模型预测”“资料完整度”或“人工标签统计”。

### 2.3 候选状态

建议保留以下互不混淆的状态：

- 候选：尚未经过人工判断。
- 想去：用户精挑细选后保留的目标。
- 黑名单：用户确认的低价值目标。
- 连锁店：已确认需要屏蔽的连锁品牌或门店。
- 资料不足：缺少评分或关键字段，需要人工复核。

---

## 3. 黑名单系统

### 3.1 功能

每家店增加“加入黑名单”按钮。操作时要求选择至少一个原因：

- 低价值目标
- 评分或口碑问题
- 环境问题
- 价格不合适
- 信息错误
- 店铺已关闭
- 不符合小馆定位
- 重复 POI
- 其他

支持：

- 撤销黑名单。
- 查看全部黑名单。
- 按原因筛选。
- 黑名单默认不出现在候选列表中。
- 用户可以临时开启“显示黑名单”用于检查误判。

### 3.2 机器学习用途

黑名单记录作为负样本保存，不只执行界面隐藏。每条记录应包含：

- POI ID、平台和商户名称。
- 标注结果和原因。
- 标注时间。
- 标注时的原始商户快照。
- 当时的环境特征快照。
- 特征版本与模型版本。
- 标注来源：用户、规则或模型。

模型不能直接覆盖人工判断。模型发现疑似低价值目标时，应进入“待复核”状态。

---

## 4. 连锁店系统

### 4.1 功能

每家店增加“标记为连锁店”按钮，支持：

- 标准化品牌名称。
- 记录原始门店名称。
- 屏蔽当前门店。
- 屏蔽同品牌全部门店。
- 撤销或纠正系统的连锁误判。
- “显示连锁店”开关。

### 4.2 识别策略

连锁识别由三层组成：

1. 内置品牌规则库。
2. 用户标注形成的品牌库。
3. 模型根据店名、分店后缀、重复门店、地址和联系方式给出的连锁嫌疑。

模型只输出嫌疑和依据。人工确认后，品牌才能进入永久屏蔽库。

黑名单和连锁店必须分开保存，避免将“连锁属性”错误解释为“低质量”。

---

## 5. 地图与接口调用额度

### 5.1 显式刷新

- 拖动或缩放地图只更新“待搜索区域”。
- 地图移动后显示“区域已变化”。
- 用户点击“搜索此区域”后才请求高德接口。
- 切换城市属于用户主动操作，可以自动执行一次搜索。
- 调整搜索半径或关键词后，由用户点击搜索按钮提交。

### 5.2 调用优化

- 相同坐标、半径、关键词和筛选条件使用短期缓存。
- 地图中心坐标按合理精度取整，避免微小偏移导致重复请求。
- 合并正在进行的相同请求。
- 记录每次查询使用的外部接口及次数。
- 页面显示数据更新时间和“使用缓存/重新获取”状态。
- 环境特征按 POI 缓存，设置较长更新时间，避免每次搜索重复计算。

---

## 6. 机器学习特征体系

### 6.1 商户自身特征

- 高德真实评分。
- 人均消费及是否缺失。
- 商户类别与标签。
- 营业时间。
- 电话、图片、地址等资料完整度。
- 店名结构、分店后缀和连锁嫌疑。
- 所属商圈、社区和 AOI。
- 数据更新时间。

### 6.2 道路环境特征

- 最近道路名称与距离。
- 最近主干道距离。
- 普通道路和主干道数量。
- 周边道路交叉口数量与密度。
- 最近交叉口距离。
- 道路结构是否复杂。

高德逆地理编码可返回道路、路口、AOI 和附近 POI，并可筛选主干道。主干道与普通道路的差异可形成特征。

### 6.3 周边设施特征

分别统计 250 米、500 米和 1 公里范围内的数量与最近距离：

- 医院和诊所。
- 火车站、高铁站和长途客运站。
- 垃圾处理、废品回收和环境卫生设施。
- 地铁站、公交枢纽和停车场。
- 学校和大学。
- 写字楼、产业园区和政府机构。
- 住宅区和社区。
- 商场、超市和菜市场。
- 景区、酒店和娱乐场所。

医院、车站等特征不预设绝对好坏。它们可能同时影响客流、租金、噪声、卫生环境和停车条件，最终权重由人工标签和训练结果决定。

### 6.4 城中村等区域特征

高德没有稳定的“城中村”直接字段。可以结合以下数据产生“区域形态推测”：

- AOI 和社区类型。
- 住宅、出租屋、学校、市场等 POI 密度。
- 道路与路口密度。
- 主干道距离。
- 商圈和行政区信息。

该结果必须标记为推测特征，不作为确定事实展示。

### 6.5 模型输出

模型建议输出：

- 候选价值概率。
- 低价值概率。
- 连锁嫌疑概率。
- 资料不足程度。
- 影响最大的特征及方向。

模型分数不得显示为高德、大众点评或其他平台的真实评分。

---

## 7. “想去”列表

“想去”是用户人工精选的高价值正样本，应独立于普通收藏保存。

功能包括：

- 加入或移出“想去”。
- 按城市、类别和添加时间筛选。
- 保存加入时的评分、价格、地址和环境特征快照。
- 显示数据是否过期。
- 支持用户主动更新单家店铺信息。
- 为获得授权的数据源预留按日或按周更新任务。

“想去”标签可以作为模型正样本，但训练时需要防止某一城市或品类样本过度集中。

---

## 8. 评论与大众点评数据

### 8.1 评论展示原则

获得合规评论数据后，应优先展示：

- 原始评论正文。
- 原始评分。
- 评论时间。
- 数据来源。
- 商户页面链接。
- 最近更新时间。

机器生成的摘要、情感分类和关键词只能作为辅助信息。用户始终能够查看原文并自行判断。

### 8.2 大众点评接入边界

当前不直接开发未经授权的大众点评网页爬虫。用户点击“获取”仍属于程序化抓取，定时抓取“想去”列表会进一步增加账号、知识产权和服务协议风险。

禁止采用：

- 绕过验证码或反爬措施。
- 破解动态字体或加密参数。
- 保存并复用用户登录 Cookie。
- 模拟虚假访问、收藏、评价或其他用户行为。
- 高频轮询和隐藏式定时抓取。

### 8.3 V3 可先实现的方案

- 每家店提供“在大众点评查看”入口。
- 用户录入大众点评商户链接和评分。
- 用户粘贴少量用于个人判断的关键评论，并记录来源链接与时间。
- 建立“想去列表待复核”队列，按设定周期提醒用户更新。
- 支持导入用户合法取得的数据文件。

### 8.4 获得官方授权后的自动化

申请美团技术服务合作中心或其他正式授权接口。获得接口权限后，启用：

- 用户点击后立即更新单家店铺。
- “想去”列表按日或按周更新。
- 请求限速、缓存和失败重试。
- 商户名称、城市、地址、电话和坐标的多字段匹配。
- 匹配置信度不足时要求用户确认。
- 保存来源、更新时间和接口版本。
- 评论原文展示与历史变化记录。

---

## 9. 建议的数据结构

### 9.1 商户表 `restaurants`

- `provider`
- `provider_poi_id`
- `name`
- `normalized_name`
- `longitude` / `latitude`
- `address`
- `city_adcode`
- `category`
- `rating`
- `average_cost`
- `last_fetched_at`

### 9.2 用户标签表 `restaurant_labels`

- `restaurant_id`
- `label_type`：want / blacklist / chain / reviewed
- `reason_code`
- `note`
- `source`：user / rule / model
- `created_at`
- `revoked_at`

### 9.3 特征表 `restaurant_features`

- `restaurant_id`
- `feature_version`
- `feature_payload`
- `source_snapshot`
- `calculated_at`
- `expires_at`

### 9.4 外部来源表 `external_sources`

- `restaurant_id`
- `provider`
- `external_url`
- `external_shop_id`
- `match_confidence`
- `rating`
- `review_count`
- `last_checked_at`
- `access_method`：manual / import / authorized_api

### 9.5 评论表 `reviews`

- `external_source_id`
- `external_review_id`
- `rating`
- `content`
- `published_at`
- `source_url`
- `fetched_at`

仅在数据来源明确允许存储评论正文时启用该表。

---

## 10. 页面修改

### 地图区域

- 增加“区域已变化”提示。
- 将“刷新真实数据”改为“搜索此区域”。
- 显示上次搜索区域和当前待搜索区域。
- 地图拖动时不发起请求。

### 店铺详情

- 增加“想去”“加入黑名单”“标记连锁店”三个主要操作。
- 展示人工标签、模型建议和依据。
- 展示环境特征卡片。
- 展示真实评分来源和更新时间。
- 增加大众点评链接及人工录入入口。
- 获得授权数据后展示原始评论列表。

### 管理页面

- 想去列表。
- 黑名单列表。
- 连锁品牌库。
- 待人工复核列表。
- 标注统计和数据导出。
- 外部数据更新状态。

---

## 11. 开发阶段

### 第一阶段：交互与标注

1. 移除距离准入逻辑。
2. 地图改为显式搜索。
3. 完成黑名单、连锁店和想去标签。
4. 建立持久化数据表和管理页面。

### 第二阶段：环境特征

1. 接入道路、路口和 AOI 信息。
2. 计算医院、车站、垃圾处理设施等距离和密度。
3. 建立特征缓存与版本管理。
4. 在详情页展示原始特征依据。

### 第三阶段：模型

1. 导出人工标注训练集。
2. 建立可解释的基线模型。
3. 模型只产生待复核建议。
4. 根据人工纠正持续更新训练数据。

### 第四阶段：评论数据

1. 先上线大众点评链接、人工录入和复核提醒。
2. 申请正式接口授权。
3. 获得授权后接入评论与评分自动更新。
4. 为想去列表启用低频、可配置的定时更新。

---

## 12. 验收标准

- 拖动地图不会产生高德 POI 请求。
- 只有点击“搜索此区域”、切换城市或明确刷新时才调用接口。
- 距地图中心不会影响候选资格和模型标签。
- 黑名单、连锁店和想去标签可以持久保存、撤销和导出。
- 黑名单与连锁店保持独立语义。
- 每个模型建议均能展示主要特征依据。
- 外部评分和评论均显示来源及更新时间。
- 没有评论数据时不生成评论数量、评论原文或情感结论。
- 未获得大众点评授权时不运行网页爬虫或定时抓取。
- 获得授权后，定时更新可以配置频率、暂停并查看运行记录。

